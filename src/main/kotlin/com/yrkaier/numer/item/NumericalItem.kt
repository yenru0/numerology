package com.yrkaier.numer.item

import net.minecraft.item.Item
import net.minecraft.item.ItemGroup


class One(properties: Properties) : Item(properties) {
    init {
        properties.maxDamage(64)
        properties.set
    }
}